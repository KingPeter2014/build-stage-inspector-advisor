# requirements.txt — shared base dependencies for ALL provider stacks.
#
# Usage:
#   Open-source:  pip install -r requirements.txt -r providers/open_source/requirements.txt
#   Azure:        pip install -r requirements.txt -r providers/azure/requirements.txt
#   AWS:          pip install -r requirements.txt -r providers/aws/requirements.txt
#   GCP:          pip install -r requirements.txt -r providers/gcp/requirements.txt

# ── Core runtime ──────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
tenacity>=8.2.0
structlog>=24.0.0

# ── API framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── OpenTelemetry (shared tracing layer — all providers export via OTel) ──────
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp>=1.24.0

# ── Semantic caching (shared SemanticCache logic) ─────────────────────────────
sentence-transformers>=3.0.0
numpy>=1.26.0
redis>=5.0.0                            # Client used by all providers (different managed services)

# ── Guardrails base (shared rule engine) ─────────────────────────────────────
# nemoguardrails is OSS-only; base rule engine uses stdlib re only.

# ── Evaluation (shared eval harness + golden datasets) ───────────────────────
deepeval>=1.0.0
ragas>=0.1.14
rouge-score>=0.1.2

# ── Document parsing (shared by all source connectors) ───────────────────────
pypdf>=4.0.0
beautifulsoup4>=4.12.0
tiktoken>=0.7.0

# ── Governance (shared — no cloud SDKs) ──────────────────────────────────────
# All governance modules use only stdlib + pydantic — already covered above.

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
# httpx is also used by tests/smoke/ for post-deploy smoke tests against live gateways
# (already listed above under Core runtime)
