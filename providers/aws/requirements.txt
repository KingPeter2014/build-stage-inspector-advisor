# providers/aws/requirements.txt
# Install ON TOP OF the root requirements.txt for the AWS AgentCore stack.
# pip install -r requirements.txt -r providers/aws/requirements.txt

# ── AWS SDK ───────────────────────────────────────────────────────────────────
boto3>=1.34.0
botocore>=1.34.0

# ── Amazon OpenSearch (vector store) ─────────────────────────────────────────
opensearch-py>=2.6.0
requests-aws4auth>=1.3.0

# ── Amazon SageMaker (experiments + registry + fine-tuning) ──────────────────
sagemaker>=2.220.0

# ── AWS X-Ray (tracing) ───────────────────────────────────────────────────────
aws-xray-sdk>=2.14.0

# ── Amazon ElastiCache / Redis ────────────────────────────────────────────────
redis>=5.0.0

# ── Document parsing (shared with OSS) ───────────────────────────────────────
pypdf>=4.0.0
beautifulsoup4>=4.12.0
