"""
providers - deployment target adapters.

Only the open-source runtime target is supported. Data ingestion may still be
extended with local, cloud storage, SharePoint, database, stream, or web
connectors without adding cloud deployment targets back into the runtime.
"""

SUPPORTED_PROVIDERS = ["open_source"]
